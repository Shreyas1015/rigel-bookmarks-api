/** Cross-cutting operation timeouts (ms). Config layer — imported wherever a deadline is needed. */

export const DB_QUERY_MS = 5000
export const EXTERNAL_API_MS = 10000
export const REDIS_MS = 1000
